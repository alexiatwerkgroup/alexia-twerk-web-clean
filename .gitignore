*.zip
*.rar
*.7z
__MACOSX/
.DS_Store

# Claude operational docs (session memory, not project source)
CLAUDE_RULES.md
CLAUDE_VIEW.html
CLAUDE_VIEW.md

# Generated preview frames / thumbnails
_preview_frames/
profile_frames/

# Legacy push/batch scripts (one-off session artifacts)
PUSH_THEN_PA.ps1
PUSH_TANDA_A.ps1
PUSH_NOW.bat

# Temp preview HTMLs (regenerated from source pages)
PACK_PREVIEWS.html
PACK_PREVIEW.html
alexia-twerk-leaks-PREVIEW.html

# Stray scratch files
testwrite.txt
textareas.txt
olve*
tatus*

# 🔐 SECRETS · NEVER COMMIT
# Local credentials reference (each developer has their own copy)
SECRETS-LOCAL.md
SECRETS.md
secrets.md
.secrets

# Environment variables (local only · production goes in Cloudflare Pages dashboard)
.env
.env.local
.env.*.local
.env.production
.env.development

# Common credential files that should never reach git
credentials.json
credentials.txt
.credentials
*.pem
*.key
id_rsa
id_ed25519
serviceAccount.json
firebase-key.json
gcp-credentials.json
aws-credentials

# IDE secret stores
.vscode/settings.json.local
.idea/workspace.xml
