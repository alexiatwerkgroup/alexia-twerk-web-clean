// twerkhub-universal-inject.js — DECOMMISSIONED 2026-04-25
// This script previously injected a duplicate <nav class="twerkhub-topbar">
// element at runtime, which produced two navbars stacked on every page.
// It has been intentionally emptied. The canonical navbar (TWK_NAV_V1) is
// now baked into every HTML file at build time by standardize_navbars.py.
// Do not restore this file's behavior.
(function () { /* no-op */ })();
