/* ═══ TWERKHUB · token pill loader · NO-OP (2026-05-06) ═══
 * Reverted to the default floating pill position (top:82px right:18px from
 * twerkhub-tokens.css) so the pill looks IDENTICAL on every page of the
 * platform. No relocation, no in-nav class, nothing.
 * File kept (instead of deleted) so any HTML that already references it
 * doesn't 404.
 */
(function(){ /* intentionally empty */ })();
