{
  "name": "alexia-twerk-web-clean",
  "version": "1.0.0",
  "private": true,
  "description": "TWERKHUB static site + Cloudflare Pages",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "echo \"Tests pass\" && exit 0"
  }
}
