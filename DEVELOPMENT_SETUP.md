# TWERKHUB Development Setup Guide

Complete guide for setting up the TWERKHUB development environment locally.

## Prerequisites

Before starting, ensure you have installed:

- **Node.js**: Version 18+ ([download](https://nodejs.org/))
- **npm**: Comes with Node.js
- **Git**: Version control ([download](https://git-scm.com/))
- **Docker & Docker Compose**: For containerized development ([download](https://www.docker.com/products/docker-desktop))
- **Supabase CLI**: For database management (`npm install -g supabase`)

## Quick Start (with Docker)

The fastest way to get started is using Docker Compose, which sets up everything automatically.

```bash
# Clone the repository
git clone <your-repo-url>
cd twerkhub

# Copy environment template
cp .env.example .env.local

# Start all services
docker-compose up -d

# Initialize database migrations
docker exec twerkhub-postgres psql -U postgres -d postgres -f /docker-entrypoint-initdb.d/01-init.sql

# Install dependencies
npm install

# Start development server (already running, but ensure it's ready)
# The app is available at http://localhost:3000
```

View logs for any service:

```bash
# View all logs
docker-compose logs -f

# View specific service logs
docker-compose logs -f app
docker-compose logs -f postgres
```

## Manual Setup (without Docker)

If you prefer not to use Docker, follow these steps:

### 1. Install Dependencies

```bash
npm install
```

### 2. Set Up Supabase Locally

```bash
# Install Supabase CLI
npm install -g supabase

# Initialize Supabase in your project
supabase init

# Start Supabase local development server
supabase start

# This will output your local credentials
```

### 3. Configure Environment Variables

Create a `.env.local` file in the project root with these variables:

```env
# Supabase (from supabase start output)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-from-supabase
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-from-supabase

# Stripe (use test keys)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-test-key
STRIPE_SECRET_KEY=sk_test_your-test-key
STRIPE_WEBHOOK_SECRET=whsec_test_your-test-secret

# Application
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development

# Database (if using separate PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres
```

### 4. Run Database Migrations

```bash
# Using Supabase CLI
supabase db push

# Or manually execute the migration SQL
psql postgresql://postgres:postgres@localhost:5432/postgres < supabase/migrations/001_init.sql
```

### 5. Start Development Server

```bash
npm run dev
```

The application will be available at `http://localhost:3000`

## Environment Variables

### Required Variables

- `NEXT_PUBLIC_SUPABASE_URL`: Your Supabase project URL
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`: Supabase anonymous public key
- `SUPABASE_SERVICE_ROLE_KEY`: Supabase service role key (keep secret)
- `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`: Stripe publishable key (safe to expose)
- `STRIPE_SECRET_KEY`: Stripe secret key (keep secret)
- `STRIPE_WEBHOOK_SECRET`: Stripe webhook signing secret

### Optional Variables

- `SENDGRID_API_KEY`: For email functionality
- `SENTRY_DSN`: For error tracking
- `JWT_SECRET`: For JWT token signing (auto-generated by Supabase)

For complete details, see [ENV_SETUP.md](./ENV_SETUP.md)

## Development Workflow

### Running Tests

```bash
# Run all tests
npm run test

# Run tests in watch mode
npm run test:watch

# Run specific test file
npm run test -- specific-test.test.ts

# Generate coverage report
npm run test:coverage
```

### Code Quality

```bash
# Run ESLint
npm run lint

# Fix linting issues
npm run lint:fix

# Format code
npm run format

# Check formatting
npm run format:check

# Type checking
npm run type-check
```

### Building for Production

```bash
# Build the application
npm run build

# Start production server
npm start
```

## Database Management

### View Database Schema

```bash
# Using Supabase Studio (web interface)
supabase studio

# Or using psql
psql postgresql://postgres:postgres@localhost:5432/postgres
```

### Run Migrations

```bash
# Create new migration
supabase migration new add_new_table

# Apply migrations
supabase db push

# Reset database (careful!)
supabase db reset
```

### Seed Data

```bash
# Seed development database with sample data
psql postgresql://postgres:postgres@localhost:5432/postgres < supabase/seeds/01-init.sql
```

## Testing Stripe Locally

### Using Stripe CLI

```bash
# Login to your Stripe account
stripe login

# Listen for Stripe events and forward to localhost
stripe listen --forward-to localhost:3000/api/webhooks/stripe

# View your webhook signing secret (displayed in terminal)
# Copy this to STRIPE_WEBHOOK_SECRET in .env.local
```

### Test Stripe Payments

```bash
# Use test card numbers
# Successful payment: 4242 4242 4242 4242
# Declined payment: 4000 0000 0000 0002
# Requires authentication: 4000 0025 0000 3155

# Expiry: Any future date (e.g., 12/25)
# CVC: Any 3 digits (e.g., 123)
```

## API Testing

### Using cURL

```bash
# Test authentication
curl -X POST http://localhost:3000/api/auth/signup \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password123"}'

# Test protected endpoint
curl -X GET http://localhost:3000/api/profile \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

### Using Postman/Insomnia

1. Import the API collection from `docs/api-collection.json`
2. Set environment variables for your local setup
3. Test endpoints directly in the GUI

## Debugging

### Browser DevTools

```bash
# The app runs with source maps enabled in development
# Open browser DevTools (F12) to debug frontend code
```

### Server-Side Debugging

```bash
# Run with Node debugger
node --inspect-brk ./node_modules/.bin/next dev

# Or use your IDE's built-in debugger
# VS Code: Press F5 to start debugging (requires .vscode/launch.json)
```

### Logs

```bash
# View application logs
tail -f logs/app.log

# View error logs
tail -f logs/error.log

# Docker logs
docker-compose logs -f app
```

## Troubleshooting

### Port Already in Use

```bash
# Find process using port 3000
lsof -i :3000

# Kill process
kill -9 <PID>

# Or use different port
PORT=3001 npm run dev
```

### Database Connection Failed

```bash
# Check PostgreSQL is running
docker ps | grep postgres

# Verify connection string in .env.local
# Format: postgresql://user:password@host:port/database

# Test connection
psql "postgresql://postgres:postgres@localhost:5432/postgres"
```

### Supabase Issues

```bash
# Reset Supabase
supabase stop
supabase start

# Check Supabase status
supabase status

# View Supabase logs
supabase logs postgres
```

### Docker Issues

```bash
# Clean up Docker
docker-compose down -v

# Rebuild containers
docker-compose build --no-cache

# Start fresh
docker-compose up -d
```

## Development Tools

### Recommended VSCode Extensions

- **ESLint**: Microsoft.vscode-eslint
- **Prettier**: esbenp.prettier-vscode
- **TypeScript Vue Plugin**: Vue.vscode-typescript-vue-plugin
- **Tailwind CSS IntelliSense**: bradlc.vscode-tailwindcss
- **Thunder Client**: rangav.vscode-thunder-client (API testing)
- **SQLTools**: mtxr.sqltools (Database management)

### File Structure

```
twerkhub/
├── src/
│   ├── app/              # Next.js App Router pages
│   ├── components/       # React components
│   ├── lib/             # Utility functions
│   ├── hooks/           # Custom React hooks
│   ├── types/           # TypeScript types
│   └── styles/          # Global styles
├── supabase/
│   ├── migrations/      # Database migrations
│   ├── seeds/           # Seed data
│   └── config.toml      # Supabase configuration
├── public/              # Static assets
├── tests/               # Test files
├── .env.local           # Environment variables (not committed)
├── docker-compose.yml   # Docker composition
├── Dockerfile           # Production Docker image
└── next.config.js       # Next.js configuration
```

## Git Workflow

### Branch Naming

- `main`: Production-ready code
- `develop`: Development branch
- `feature/feature-name`: New features
- `bugfix/bug-name`: Bug fixes
- `hotfix/hotfix-name`: Production hotfixes

### Commit Messages

```
feat: Add new video player component
fix: Resolve token calculation bug
docs: Update setup instructions
style: Format code with prettier
refactor: Optimize database queries
test: Add tests for auth flow
```

### Pull Request Process

1. Create feature branch from `develop`
2. Make changes and commit
3. Push to remote and create PR
4. Wait for CI/CD checks to pass
5. Request code review
6. Address feedback and update
7. Merge when approved

## Performance Optimization

### Build Optimization

```bash
# Analyze bundle size
npm run build -- --analyze

# Check performance metrics
npm run test:perf
```

### Database Optimization

```bash
# Check slow queries
supabase logs postgres | grep slow

# Run EXPLAIN on queries
EXPLAIN ANALYZE SELECT * FROM profiles;
```

## Useful Commands

```bash
# Development
npm run dev              # Start dev server
npm run build           # Build for production
npm start               # Start production server

# Testing
npm run test            # Run tests
npm run test:watch      # Run tests in watch mode
npm run test:coverage   # Generate coverage

# Code Quality
npm run lint            # Run ESLint
npm run lint:fix        # Fix linting issues
npm run format          # Format code
npm run format:check    # Check formatting
npm run type-check      # TypeScript type checking

# Database
supabase start          # Start local Supabase
supabase stop           # Stop local Supabase
supabase db push        # Apply migrations
supabase db reset       # Reset database

# Docker
docker-compose up       # Start services
docker-compose down     # Stop services
docker-compose logs     # View logs
docker-compose ps       # List services
```

## Getting Help

- **Documentation**: See [docs/](./docs/) directory
- **API Documentation**: See [docs/API.md](./docs/API.md)
- **Deployment**: See [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md)
- **Environment Setup**: See [ENV_SETUP.md](./ENV_SETUP.md)
- **Issues**: Check [GitHub Issues](https://github.com/your-repo/issues)
- **Team**: Reach out to dev@twerkhub.com

## Next Steps

1. Complete the quick start above
2. Review [API.md](./docs/API.md) for API endpoints
3. Check [CODING_STANDARDS.md](./CODING_STANDARDS.md) for code guidelines
4. Start with a small feature or bug fix
5. Follow the Git workflow above for PRs
