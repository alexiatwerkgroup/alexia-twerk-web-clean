# Session log · 2026-05-09

Comprehensive list of every fix shipped during the marathon session of
2026-05-09. Use this as a reference if anything regresses.

---

## Backend / Auth / DB

1. Migración total Supabase → Cloudflare D1 (auth, profile, tokens, comments, heatmap, admin, email tokens).
2. Resend integration for password reset + email verification.
3. Google OAuth with PKCE (S256 code_challenge_method).
4. Cloudflare R2 for avatar storage (`twerkhub-avatars` bucket).
5. JWT HS256 + HttpOnly cookie + PBKDF2 password hashing (Web Crypto).
6. `POST /api/tokens/grant` endpoint with cap (max 1000/call) and atomic increment.
7. `/api/profile/me`, `/api/auth/session`, `/api/auth/signin`, `/api/auth/signup`, `/api/auth/forgot-password`, `/api/auth/reset-password`.
8. Schema: `users`, `profiles`, `video_comments`, `comment_reports`, `video_heatmap`, `user_video_views`, `auth_tokens`.
9. Paused Supabase project (26TB egress restricted).

## Founder override (alexiatwerkoficial@gmail.com / Firestarter)

10. 1,000,009 tokens floor, tier `vip`, member since `2018-01-01`.
11. Accumulation on top of the floor (display = max(FLOOR, balance)).
12. D1 update: Firestarter tokens 850 → 1,000,009, tier basic → vip.

## Tier thresholds (canonical)

13. Aligned `3000 / 9000 / 50000` across `account.html` + `functions/_lib/tier.js` + `assets/twk-tokens-v3.js` + `assets/profile-stats-live.js`.
14. Recomputed tier of all D1 users.

## Login / Sesión

15. Joncito: login by username (old RPC `email_for_username` returned null in shim).
16. Password reset directly in D1 with PBKDF2.
17. `clearToken` wipes 15+ identity localStorage keys on signOut (no more leak founder→next user).
18. `isFounder()` only trusts email (not localStorage `alexia_role`).
19. Hard sync DB→local on login (no more MAX merging stale local).
20. SW does NOT cache `/api/*` (random logout).
21. Boot session does NOT clearToken on transient failures (only on explicit 401/403).

## Service Worker

22. Changed from stale-while-revalidate → network-first for JS/CSS.
23. Bumped to `v2.1.0` + nuke caches in activate event.
24. One-shot cache nuke script inlined in every HTML head.

## Toast notifications

25. Spans collapsed inline → divs + `textContent` + appendChild + inline styles.
26. Emergency CSS auto-injected by `twerkhub-tokens.js`.
27. Inline `<style>` with `!important` in every HTML head.
28. LOUD coin-cascade ping at `0.85` master gain (5 notes + 110Hz sub-bass).

## Tokens earnings

29. Drift reconciler reading `/api/profile/me`, pushing local-only delta to server in chunks of 1000.
30. `videoWatch` reward 10 → 15.
31. Daily caps: pages 10→50, watches 10→20, finishes 5→10.
32. Cap "burning" reward bug fixed — only mark visited/started AFTER grant succeeds.
33. Founder bypass for daily caps + dedup (rolled back to normal flow with `videoWatch=15` for everyone).
34. Auto-grant on video page load: +15 watch at 1.2s, +30 finish at 25s.
35. `getState()` exposes `visited` / `videos` / `shares` / `welcomed` / `lastLogin` for the dashboard achievements.

## Thumbnails

36. `thumb-fallback.js` cascade extended to 9 variants: `maxresdefault → sddefault → hqdefault → mqdefault → default → 0 → 1 → 2 → 3`.
37. `markDead()` is now a no-op (no more X+VIDEO UNAVAILABLE overlay).
38. `thumb-unavailable.svg` reduced to a 1x1 transparent pixel.
39. `unmarkDead()` recovers via `data-vid` / parent `<a>` href / stored `data-twk-orig-src`.
40. Force eager loading + reset of `src` to bypass lazy-loading state.
41. Inline force-thumbs CSS + JS in every HTML head.

## Locked videos / paywall

42. Auto-unlock script: iframe behind paywall overlay (no more replacement).
43. Founder / premium / VIP automatically hide the overlay.

## Age gate

44. Skip for logged-in users.
45. Skip for OAuth / verify flow URL params.
46. Scroll to top before showing the modal.

## Email capture popup

47. No-blur (removed `backdrop-filter`).
48. Skip for logged-in users.
49. Skip on home / portal.

## Broken pages / URLs

50. `/cosplay-fancam-leaks/` redirects to `/hottest-cosplay-fancam/`.
51. Interlinks updated in 30+ HTMLs.
52. `profile.html`: CSS comment with `<head>` matched the regex → injection broke the comment block.
53. `account.html`: same comment-collision bug.
54. 30+ HTMLs cleaned of duplicate kill-script + emergency-style injections.

## Encoding / Git

55. Stripped UTF-8 BOM from 98 HTML files (was blocking commits via pre-commit hook).
56. CRLF→LF warnings (non-fatal, normalized on next git touch).

## D1 direct edits

57. Firestarter: tokens, total_earned, tier set to founder values.
58. Joncito: tier corrected to `basic`.
59. Firestarter password reset (PBKDF2 hash regenerated).
60. Joncito password reset.

## Profile page

61. Avatar upload via R2 with base64 fallback.
62. Bio saving wired to `POST /api/profile/me`.
63. Username no longer overwritten by email-prefix (mirror() reads `auth-v3` priority 0).
64. Defender (every 500ms for 8s) overwrites h1 + avatar from `/api/profile/me`.
65. `enter-now-widget.js` syntax error commented out of `profile.html`.

## Misc

66. Hero loop start/stop times for cards `X-lPzSuvf3k`, `phlPRrYpmz0`.
67. Anti-skip optimizations + `clearWatchdog()` cleanup.
68. Pre-buffer card #2 VR180 3D.
69. Internal traffic exclusion in GA4 (IP `181.21.19.236`).
70. Plus a non-trivial number of cache-buster bumps and PowerShell deploys.

## Hardening shipped at end of session

71. `assets/twk-guardian.js` — runs first on every page, validates invariants, self-heals known issues, exposes `__twkGuardian.selfTest()`.
72. `CRITICAL_INVARIANTS.md` — 12 frozen rules with affected files and rationale.
73. `EMERGENCY_PLAYBOOK.md` — recovery procedures (this file's neighbor).
74. `POWERSHELL_RECIPES.md` — canonical scripts that don't break HTML.
